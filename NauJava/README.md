# TestMaster — Система тестирования и оценки знаний

Система для создания, прохождения и автоматической оценки тестов. Поддерживает роли пользователей: `USER`, `CREATOR`, `ADMIN`.

---

## 🛠️ Стек технологий

- **Язык**: Java 21
- **Фреймворк**: Spring Boot 3.5.6
- **База данных**: PostgreSQL 17.4
- **ORM**: Spring Data JPA + Hibernate
- **Безопасность**: Spring Security (BCrypt, ролевой доступ)
- **Frontend**: Thymeleaf
- **Тестирование**: JUnit 5, Mockito
- **Сборка**: Maven
- **Контейнеризация**: Docker, Docker Compose
- **Документация API**: Swagger (Springdoc OpenAPI)

---

## Сборка и запуск

1. Собрать проект:
   ```bash
   ./mvnw clean package -DskipTests

2. Собрать проект:
   ```bash
    docker build -t testmaster .

3. Запустить:
    ```bash
   docker compose up --build

---

### Требования
- JDK 21
- Maven
- Docker Desktop (для запуска в контейнере)

---

### Документация API
API автоматически документируется через Swagger:
http://localhost:8080/swagger-ui/index.html

---

### Автор
Костарева Анастасия